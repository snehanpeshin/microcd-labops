"use client";
import { useEffect,useMemo,useState } from "react";
import { BookOpen,Download,FileLock2,FilePlus2,Heart,Search,ShieldCheck,Upload } from "lucide-react";
import { decryptLocalDocument,encryptLocalDocument,type EncryptedPayload } from "@/lib/documents/crypto";
import { deleteEncryptedRecord,listEncryptedRecords,saveEncryptedRecord } from "@/lib/documents/local-store";
import { documentTemplates,searchTemplates,type DocumentTemplate } from "@/lib/documents/templates";
type Draft={id:string;title:string;domain:string;status:"Draft";version:string;sections:Array<{title:string;content:string}>;updatedAt:string};
export function DocumentHub(){const[query,setQuery]=useState("");const[domain,setDomain]=useState("All");const[selected,setSelected]=useState<DocumentTemplate|null>(null);const[passphrase,setPassphrase]=useState("");const[drafts,setDrafts]=useState<Array<{id:string;updatedAt:string;payload:EncryptedPayload}>>([]);const[message,setMessage]=useState("");const[favorites,setFavorites]=useState<string[]>([]);const results=useMemo(()=>searchTemplates(query,domain),[query,domain]);const refresh=()=>listEncryptedRecords().then(setDrafts).catch(()=>setDrafts([]));useEffect(()=>{void refresh();},[]);
async function createDraft(){if(!selected)return;try{const draft:Draft={id:crypto.randomUUID(),title:selected.title,domain:selected.domain,status:"Draft",version:"0.1",sections:selected.sections.map(title=>({title,content:"[Complete this section]"})),updatedAt:new Date().toISOString()};await saveEncryptedRecord(draft.id,await encryptLocalDocument(draft,passphrase));setMessage("Encrypted draft saved only on this device.");setSelected(null);await refresh();}catch(e){setMessage(e instanceof Error?e.message:"Could not save draft");}}
async function exportDraft(row:{id:string;payload:EncryptedPayload}){const blob=new Blob([JSON.stringify({format:"microcd-labops-encrypted-export",payload:row.payload},null,2)],{type:"application/json"});const a=document.createElement("a");a.href=URL.createObjectURL(blob);a.download=`labops-encrypted-${row.id}.json`;a.click();URL.revokeObjectURL(a.href);}
async function inspect(row:{payload:EncryptedPayload}){try{const draft=await decryptLocalDocument<Draft>(row.payload,passphrase);setMessage(`Unlocked: ${draft.title}. Content remains on this device.`);}catch(e){setMessage(e instanceof Error?e.message:"Could not unlock draft");}}
return <div className="document-hub"><section className="document-hero"><div><p className="eyebrow">Documentation intelligence hub</p><h1>Create, find, and manage biotech documentation without starting from a blank page.</h1><p>Structured starter formats for laboratory, product, quality, clinical, manufacturing, and regulatory teams.</p><div className="privacy-line"><ShieldCheck size={18}/><span><strong>Local-only workspace:</strong> encrypted drafts remain on this device. External AI processing is disabled.</span></div></div><div className="document-actions"><button onClick={()=>document.getElementById("template-library")?.scrollIntoView()}><BookOpen/>Find a template</button><label><Upload/>Upload and review<input type="file" accept=".pdf,.docx,.txt" onChange={e=>setMessage(e.target.files?.[0]?`${e.target.files[0].name} selected locally. Automated extraction is not enabled.`:"")}/></label><button onClick={()=>setMessage("AI drafting requires explicit organization approval. No document content was transmitted.")}><FilePlus2/>Create with AI <small>Disabled</small></button></div></section><section className="local-key-panel"><div><strong>Local encryption passphrase</strong><p>Required to create or unlock drafts. LabOps does not receive or recover it.</p></div><input type="password" value={passphrase} onChange={e=>setPassphrase(e.target.value)} placeholder="At least 12 characters" autoComplete="off"/>{message&&<p role="status">{message}</p>}</section><section><div className="section-heading-row"><div><p className="eyebrow">On this device</p><h2>Encrypted drafts</h2></div><span>{drafts.length} local records</span></div><div className="local-draft-list">{drafts.length?drafts.map(row=><article key={row.id}><FileLock2/><div><strong>Encrypted local document</strong><p>Updated {new Date(row.updatedAt).toLocaleString()} · title encrypted</p></div><button onClick={()=>inspect(row)}>Unlock</button><button aria-label="Export encrypted backup" onClick={()=>exportDraft(row)}><Download/></button><button onClick={async()=>{if(confirm("Delete this encrypted local draft? This cannot be undone.")){await deleteEncryptedRecord(row.id);await refresh();}}}>Delete</button></article>):<p className="empty-document-state">No local drafts yet. Choose a template below to create one.</p>}</div></section><section id="template-library"><div className="section-heading-row"><div><p className="eyebrow">Template library</p><h2>Biotech documentation starters</h2><p>Starting points only. Quality, legal, and regulatory review may be required.</p></div></div><div className="template-toolbar"><label><Search/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search calibration, CAPA, 510(k)…"/></label><select value={domain} onChange={e=>setDomain(e.target.value)}><option>All</option>{[...new Set(documentTemplates.map(t=>t.domain))].map(value=><option key={value}>{value}</option>)}</select></div><div className="template-grid">{results.map(template=><article key={template.id}><div className="template-card-top"><span>{template.domain}</span><button aria-label={`Favorite ${template.title}`} onClick={()=>setFavorites(f=>f.includes(template.id)?f.filter(id=>id!==template.id):[...f,template.id])}><Heart fill={favorites.includes(template.id)?"currentColor":"none"}/></button></div><h3>{template.title}</h3><p>{template.description}</p><div className="template-meta"><span>{template.type}</span><span>{template.minutes} min</span><span>{template.status}</span></div><button className="use-template" onClick={()=>setSelected(template)}>Preview and use</button></article>)}</div></section>{selected&&<div className="document-dialog" role="dialog" aria-modal="true" aria-labelledby="template-preview-title"><div><button className="dialog-close" onClick={()=>setSelected(null)}>Close</button><p className="eyebrow">{selected.domain}</p><h2 id="template-preview-title">{selected.title}</h2><p>{selected.description}</p><h3>Required sections</h3><ol>{selected.sections.map(section=><li key={section}>{section}</li>)}</ol><p className="template-disclaimer">Starter content is not regulator-approved and does not replace official requirements.</p><button className="use-template" disabled={passphrase.length<12} onClick={createDraft}>Create encrypted local draft</button></div></div>}</div>}
