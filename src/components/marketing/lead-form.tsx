"use client";
import { useState } from "react";
import { Button } from "@/components/ui/button";
export function LeadForm() { const [state, setState] = useState<"idle"|"sending"|"sent"|"error">("idle"); async function submit(formData: FormData) { setState("sending"); const response = await fetch("/api/leads", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(Object.fromEntries(formData)) }); setState(response.ok ? "sent" : "error"); } return <form action={submit} className="form-grid"><div className="form-field"><label htmlFor="lead-name">Name</label><input id="lead-name" name="name" required minLength={2} /></div><div className="form-field"><label htmlFor="lead-email">Work email</label><input id="lead-email" name="email" type="email" required /></div><div className="form-field form-field-wide"><label htmlFor="lead-org">Organization</label><input id="lead-org" name="organization" /></div><div className="hidden" aria-hidden="true"><label htmlFor="website">Website</label><input id="website" name="website" tabIndex={-1} autoComplete="off" /></div><div className="form-field form-field-wide"><label htmlFor="lead-message">What should the pilot help you document or trace?</label><textarea id="lead-message" name="message" rows={6} minLength={20} required /></div><div className="form-field-wide flex items-center gap-4"><Button disabled={state === "sending"}>{state === "sending" ? "Sending…" : "Request a pilot discussion"}</Button>{state === "sent" && <p role="status" className="text-sm font-semibold text-emerald-800">Request received.</p>}{state === "error" && <p role="alert" className="text-sm font-semibold text-red-800">Submission is unavailable. Please email info@microcdlabs.com.</p>}</div></form>; }
