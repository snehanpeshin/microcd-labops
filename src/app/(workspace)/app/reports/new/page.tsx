import { PageHeader } from "@/components/ui/page-header";import { ReportBuilder } from "@/components/reports/report-builder";import { requireWorkspaceIdentity } from "@/lib/auth";import { listProjects } from "@/lib/data/workspace";
export default async function NewReportPage(){const identity=await requireWorkspaceIdentity();const projects=await listProjects(identity);return <><PageHeader eyebrow="Engineering reports" title="Generate a report" description="Build a structured record from project context, source evidence, server-calculated results, and explicit acceptance criteria."/><ReportBuilder projects={projects} demo={identity.demo}/></>}
