import { requireWorkspaceIdentity } from "@/lib/auth";import { listActivity } from "@/lib/data/workspace";import { Badge } from "@/components/ui/badge";import { Card } from "@/components/ui/card";import { DataTable,Td } from "@/components/ui/data-table";import { PageHeader } from "@/components/ui/page-header";
export default async function ActivityPage(){const identity=await requireWorkspaceIdentity();const activity=await listActivity(identity);return <><PageHeader eyebrow="Immutable operational history" title="Activity log" description="Important record changes are written by server-side workflows. Normal workspace users cannot edit audit entries."/><Card><DataTable caption="Activity log" headers={["Timestamp","Actor","Action","Record type","Summary"]}>{activity.map(entry=><tr key={entry.id}><Td>{new Date(entry.timestamp).toLocaleString()}</Td><Td>{entry.actor}</Td><Td><Badge>{entry.action}</Badge></Td><Td>{entry.recordType}</Td><Td>{entry.summary}</Td></tr>)}</DataTable></Card></>}
